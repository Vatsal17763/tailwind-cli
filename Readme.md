git --version
git status
git branch
git branch devlopment
git checkout devlopment
git puch origin main
git push origin master
git commit -m "tailwind-cli project"
git add .
git remote add origin https://github.com/Vatsal17763/tailwind-cli.git
git init
git commit -m "msg"

